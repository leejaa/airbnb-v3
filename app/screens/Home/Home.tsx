import React from 'react';
import { View } from 'react-native';
import { HomeStackNavProps } from '../../types';

function Home({ navigation } : HomeStackNavProps<'Home'>) {
  return (
    <View />
  );
}

export default Home;
